{
  "name": "cx-survey-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:init": "scripts/dev-db.sh init",
    "db:start": "scripts/dev-db.sh start",
    "db:stop": "scripts/dev-db.sh stop",
    "db:migrate": "scripts/dev-db.sh migrate",
    "db:reset": "scripts/dev-db.sh reset",
    "seed": "pnpm --filter @ok/web seed"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  }
}
