# Mirrors [project.dependencies] in pyproject.toml. Vercel's Python build
# installs from this file, not from pyproject.toml's dependency list — the
# build stage otherwise succeeds with nothing installed, and every request
# then crashes with ModuleNotFoundError. Keep the two lists in sync.
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.9
pandas>=2.2
numpy>=1.26
scipy>=1.13
statsmodels>=0.14
scikit-learn>=1.5
pyreadstat>=1.2
openpyxl>=3.1
python-multipart>=0.0.32
